# Bootstrap Landing Page

- Initialized the static project and kept the stack to plain HTML, CSS, and JS.
- Chose an organic gallery-style direction with restrained animated bubbles.
- Implemented accessible form validation, image preview, drag/drop, Supabase
  Storage upload, hidden public URL field, and Google Forms submission.
- Kept all external integration values in one `CONFIG` object in `script.js`.
- Verified JavaScript syntax, whitespace, static serving, desktop rendering,
  and corrected a mobile grid overflow found during screenshot review.
- Documented Vercel deployment and the security limits of frontend-only
  anonymous Storage uploads.
